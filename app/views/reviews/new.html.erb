<div class="container my-5">
  <h2>Donner mon avis 💬</h2>

  <%= form_with(model: @review, local: true, class: "mt-4") do |f| %>
    <% if @review.errors.any? %>
      <div class="alert alert-danger">
        <h5>⚠️ <%= pluralize(@review.errors.count, "erreur") %> :</h5>
        <ul>
          <% @review.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="mb-3">
      <%= f.label :title, "Titre de l’avis" %>
      <%= f.text_field :title, class: "form-control", placeholder: "Ex : Une super expérience" %>
    </div>

    <div class="mb-3">
      <%= f.label :content, "Votre avis" %>
      <%= f.text_area :content, class: "form-control", rows: 5, placeholder: "Partagez votre expérience..." %>
    </div>

    <div class="mb-3 w-25">
      <%= f.label :rating, "Note (1 à 5)" %>
      <%= f.number_field :rating, in: 1..5, class: "form-control" %>
    </div>

    <%= f.submit "Publier mon avis", class: "btn btn-primary" %>
  <% end %>
</div>


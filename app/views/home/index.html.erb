<div class="text-center">
  <h1 class="mb-3">Bienvenue sur la Ressourcerie en ligne 🌱</h1>
  <p class="lead mb-4">Facilitez vos dons en planifiant une collecte à domicile.</p>

  <% if user_signed_in? %>
    <p class="mb-3">Connecté en tant que <strong><%= current_user.email %></strong></p>
    <div class="d-flex justify-content-center gap-3">
      <%= link_to "Prendre rendez-vous", new_appointment_path, class: "btn btn-primary" %>
      <%= button_to "Se déconnecter", destroy_user_session_path, method: :delete, class: "btn btn-danger" %>
    </div>
  <% else %>
    <div class="d-flex justify-content-center gap-3">
      <%= link_to "Créer un compte", new_user_registration_path, class: "btn btn-success" %>
      <%= link_to "Se connecter", new_user_session_path, class: "btn btn-outline-primary" %>
    </div>
  <% end %>
</div>

<!-- 🌟 Section Avis -->
<div class="container my-5">
  <h2 class="text-center mb-4">Ils nous donnent leur avis 💬</h2>
  <div class="text-center mb-3">
    <% if user_signed_in? %>
      <%= link_to "Donner mon avis", new_review_path, class: "btn btn-primary" %>
    <% else %>
      <%= link_to "Connectez-vous pour donner votre avis", new_user_session_path, class: "btn btn-outline-secondary" %>
    <% end %>
  </div>

  <div class="row justify-content-center">
    <% Review.order(created_at: :desc).limit(3).each do |review| %>
      <div class="col-md-4">
        <div class="card mb-3 shadow-sm">
          <div class="card-body">
            <h5 class="card-title"><%= review.title %></h5>
            <p class="card-text"><%= truncate(review.content, length: 100) %></p>
            <p class="text-muted">⭐️ <%= review.rating %> / 5 — par <%= review.user.email.split('@').first %></p>
            <%= link_to "Lire plus", review_path(review), class: "btn btn-sm btn-outline-primary" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>

  <div class="text-center mt-4">
    <%= link_to "Voir tous les avis", reviews_path, class: "btn btn-outline-dark" %>
  </div>
</div>



